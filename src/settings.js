export class Settings {
  static player = {
    speed: 200,
    scaleW: 0.3,
    scaleH: 0.3,
  };

  static mainCamera = {
    zoom: 1,
    smoothX: 0.1,
    smoothY: 0.1,
    roundPixels: false,
  };
}
